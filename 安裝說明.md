# ExcelMaster Windows／macOS 安裝說明

本安裝包只提供給訂閱有效且已完成安裝包下載的帳號。每次下載網址 10 分鐘有效並只能使用一次；啟用碼只顯示一次，會綁定一台裝置。

## 安裝前準備

1. 從 ExcelMaster「方案與授權」頁選擇 Windows 或 macOS，按「產生下載與啟用碼」。
2. 立即保存畫面上的啟用碼，再按下載。不要把啟用碼交給其他人。
3. 安裝最新版 Docker Desktop，啟動後確認 Docker 顯示 Running。
4. ZIP 解壓縮到使用者可寫入的資料夾，不要直接在 ZIP 預覽中執行。
5. 對照下載頁提供的 SHA-256；檔案雜湊不一致時不要安裝。

## Windows

1. 雙擊 `installer/Install-ExcelMaster.cmd`。
2. 輸入授權伺服器；一般使用者直接按 Enter 使用預設值。
3. 輸入下載頁顯示的啟用碼。系統會先確認訂閱、裝置數與到期日。
4. 設定本機管理員 Email 與至少 12 字元的密碼。
5. 等待 Docker 建置完成，瀏覽器會開啟 `http://localhost:8080`。

若 Windows 阻擋執行，請確認 ZIP 來自 ExcelMaster、SHA-256 正確，再以滑鼠右鍵選擇內容／解除封鎖。不要停用防毒軟體。

## macOS

1. 安裝並啟動 Docker Desktop。
2. 在 Terminal 進入解壓縮資料夾，執行 `chmod +x installer/Install-ExcelMaster.command`。
3. 雙擊該檔或在 Terminal 執行 `./installer/Install-ExcelMaster.command`。
4. 依序輸入啟用碼、本機管理員 Email 與密碼。
5. 完成後開啟 `http://localhost:8080`。

若 Gatekeeper 阻擋，請在「系統設定 → 隱私權與安全性」確認檔案來源與雜湊後允許一次；不要關閉 Gatekeeper。

## 授權與到期

- 本機 API 每 15 分鐘向 ExcelMaster 授權服務確認一次。
- 訂閱到期或裝置被管理員撤銷時，伺服器立即拒絕後續驗證。
- 僅在授權服務暫時無法連線、且之前成功驗證過時，最多容許 24 小時離線緩衝；訂閱到期不適用離線緩衝。
- 重新安裝前可請平台管理員撤銷舊裝置，再用新的下載與啟用碼安裝。

## 更新、備份與移除

- 更新前備份 Docker volumes `postgres_data` 與 `file_storage`。
- 查看狀態：`docker compose ps`。
- 查看錯誤：`docker compose logs --tail=200 api worker parser web`。
- 停止：`docker compose down`。除非已完成備份，請勿加上 `-v`，因為那會刪除資料。

## 常見問題

- 「啟用碼無效」：確認已先完成 ZIP 下載、訂閱未到期，且沒有把同一啟用碼用在另一台裝置。
- 「裝置數已達上限」：請管理員到平台後台撤銷不用的裝置。
- 「Docker 未啟動」：開啟 Docker Desktop，等狀態變成 Running 後重試。
- 「授權服務無法連線」：檢查網路、防火牆、DNS 與系統時間；不要修改 `.env` 嘗試繞過授權。
- 安裝失敗時保留畫面訊息與 `docker compose logs`，提供給支援人員，請先移除其中的 Email、啟用碼與密鑰。
